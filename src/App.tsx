export default function App() {
  return (
    <main className="p-8">
      {/* "TUTTO" in caps is the in-game event; the app itself is "Tutto". */}
      <h1 className="text-4xl font-bold text-center">Tutto</h1>
    </main>
  );
}
